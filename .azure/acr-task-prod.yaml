version: v1.1.0
steps:
  - build: >-
      -t {{.Run.Registry}}/forecast-backend-prod:{{.Run.ID}}
      -t {{.Run.Registry}}/forecast-backend-prod:latest
      -f Dockerfile .
  - push:
      - "{{.Run.Registry}}/forecast-backend-prod:{{.Run.ID}}"
      - "{{.Run.Registry}}/forecast-backend-prod:latest"
# Step below requires Contributor role on rg-forecast-prod assigned to managed identity
# principal: (see after task creation)
# Uncomment once an Azure Owner runs the role assignment
#  - cmd: >-
#      mcr.microsoft.com/azure-cli:latest
#      bash -c "az login --identity &&
#      az containerapp update
#      --name forecast-backend-prod
#      --resource-group rg-forecast-prod
#      --image {{.Run.Registry}}/forecast-backend-prod:{{.Run.ID}}"
#    identity: "[system]"
