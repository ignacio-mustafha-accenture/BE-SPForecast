version: v1.1.0
steps:
  - build: >-
      -t {{.Run.Registry}}/forecast-backend-dev:{{.Run.ID}}
      -t {{.Run.Registry}}/forecast-backend-dev:latest
      -f Dockerfile .
  - push:
      - "{{.Run.Registry}}/forecast-backend-dev:{{.Run.ID}}"
      - "{{.Run.Registry}}/forecast-backend-dev:latest"
# Step below requires Contributor role on rg-forecast-dev assigned to managed identity
# principal: (see after task creation)
# Uncomment once an Azure Owner runs the role assignment
#  - cmd: >-
#      mcr.microsoft.com/azure-cli:latest
#      bash -c "az login --identity &&
#      az containerapp update
#      --name forecast-backend-dev
#      --resource-group rg-forecast-dev
#      --image {{.Run.Registry}}/forecast-backend-dev:{{.Run.ID}}"
#    identity: "[system]"
